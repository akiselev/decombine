# Possible new behavior

Candidate units with no adequate counterpart in `v1`. Possible new behavior.

## Examples

### Example 1

- — ↔ brand_new (`v2:lib/fresh.rs` lines 1-9) (score 0.0000)

**Right:** `v2:lib/fresh.rs` lines 1-9

```rust
fn brand_new(input: u32) -> u32 {
    let doubled = input * 2;
    doubled + 1
}
```

## All records

- — ↔ brand_new (`v2:lib/fresh.rs` lines 1-9) (score 0.0000)
