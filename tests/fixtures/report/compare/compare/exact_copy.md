# Exact copies

## Examples

### Example 1

- same (`v1:src/exact.rs` lines 1-9) ↔ same (`v2:lib/exact.rs` lines 1-9) (score 1.0000)

**Left:** `v1:src/exact.rs` lines 1-9

```rust
fn same(input: u32) -> u32 {
    let doubled = input * 2;
    doubled + 1
}
```

**Right:** `v2:lib/exact.rs` lines 1-9

```rust
fn same(input: u32) -> u32 {
    let doubled = input * 2;
    doubled + 1
}
```

## All records

- same (`v1:src/exact.rs` lines 1-9) ↔ same (`v2:lib/exact.rs` lines 1-9) (score 1.0000)
