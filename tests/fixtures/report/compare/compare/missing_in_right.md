# Possible missing coverage

Reference units with no adequate candidate in `v2`. Possible missing coverage — verify before relying on it.

## Examples

### Example 1

- legacy (`v1:src/gone.rs` lines 1-9) ↔ — (score 0.0000)

**Left:** `v1:src/gone.rs` lines 1-9

```rust
fn legacy(input: u32) -> u32 {
    let doubled = input * 2;
    doubled + 1
}
```

## All records

- legacy (`v1:src/gone.rs` lines 1-9) ↔ — (score 0.0000)
